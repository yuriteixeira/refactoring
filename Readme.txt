
Part 1
Refactor Service.php and implement on top of that a service that performs requests to a public API of your preference (IMDB, Twitter, Spotify web-api) that fetches some imformation, hanldes errors, connection issues, and caches the responses if necessary (use any cache method of preference).

Requirements 1: You must implement a generic solution that can be reused by other functions or extended by other classes.
Requirements 2: Use phpunit to test the service and others classes functionality. 

Note: No need to implement a user interface for this. 

Part 2
Given the atached psd, implement it using twig, less.
Requirements: Make it responsive. 